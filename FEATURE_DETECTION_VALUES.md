# 検出値表示機能 - 実装完了レポート

## 📝 改良内容

**リクエスト**: 検出したそのままの値（生の測定値）も表示するように改良

### 🔍 実装した変更

#### 1. **detect.py** - CSV に検出値を追加

**ファイル**: `detect_yolo/detect.py`

- **CSV ヘッダーの拡張**（line 128）:
  ```
  従来: ["メッシュID", "パス", "総合スコア", "Pitchスコア", "Yawスコア", "EARスコア"]
  新規: ["メッシュID", "パス", "総合スコア", "Pitchスコア", "Yawスコア", "EARスコア", 
         "Pitch角度(度)", "Yaw角度(度)", "EAR値"]
  ```

- **CSV 行の出力**（line 376-389）:
  ```python
  row_data = [
      face_id,
      face_filename,
      total_score,                    # 100点満点
      int(round(30 * pitch_score)),   # 30点満点
      int(round(30 * yaw_score)),     # 30点満点
      int(round(40 * ear_score)),     # 40点満点
      f"{pitch:.1f}",                 # ★新規: Pitch角度（度）
      f"{yaw:.1f}",                   # ★新規: Yaw角度（度）
      f"{ear:.3f}"                    # ★新規: EAR値
  ]
  ```

#### 2. **app3.py** - Flask で検出値を読み込み

**ファイル**: `UI/app3.py`

- **CSV 読み込み時に検出値も抽出**（line 31-44）:
  ```python
  pitch_angle = row.get('Pitch角度(度)', '').strip()    # 新規
  yaw_angle = row.get('Yaw角度(度)', '').strip()        # 新規
  ear_value = row.get('EAR値', '').strip()             # 新規
  
  image_map.append({
      ...既存フィールド...
      'pitch_angle': pitch_angle,     # 新規
      'yaw_angle': yaw_angle,         # 新規
      'ear_value': ear_value,         # 新規
  })
  ```

#### 3. **index.html** - テーブルビューに検出値を表示

**ファイル**: `UI/templates/index.html`

- **テーブルヘッダーの拡張**（line 304-312）:
  ```html
  <th>Pitch<br/>スコア</th>
  <th>Pitch<br/>角度</th>        ★新規
  <th>Yaw<br/>スコア</th>
  <th>Yaw<br/>角度</th>          ★新規
  <th>EAR<br/>スコア</th>
  <th>EAR<br/>値</th>            ★新規
  ```

- **テーブルセルへの値挿入**（line 321-325）:
  ```javascript
  `<td>${img.pitch_angle || '-'}°</td>`    // Pitch角度を表示
  `<td>${img.yaw_angle || '-'}°</td>`      // Yaw角度を表示
  `<td>${img.ear_value || '-'}</td>`       // EAR値を表示
  ```

## 📊 CSV フォーマット（新）

```csv
メッシュID,パス,総合スコア,Pitchスコア,Yawスコア,EARスコア,Pitch角度(度),Yaw角度(度),EAR値
33,face_id_33_20251208-170833_0.jpg,60,30,30,0,5.6,2.7,0.079
```

### データの意味

| 列 | 説明 | 値の範囲 | 用途 |
|-----|------|---------|------|
| **メッシュID** | 検出された顔のID | 0～∞ | 識別用 |
| **パス** | 保存された顔画像のファイル名 | - | ファイル参照 |
| **総合スコア** | 総合評価点数 | 0～100点 | 総合評価 |
| **Pitchスコア** | Pitch評価点数 | 0～30点 | 上下向き評価 |
| **Yawスコア** | Yaw評価点数 | 0～30点 | 左右向き評価 |
| **EARスコア** | EAR評価点数 | 0～40点 | 目開き評価 |
| **Pitch角度(度)** | 検出されたPitch角度（生値） | -45°～45° | 詳細分析用 |
| **Yaw角度(度)** | 検出されたYaw角度（生値） | -180°～180° | 詳細分析用 |
| **EAR値** | 検出されたEAR値（生値） | 0.0～1.0 | 詳細分析用 |

## 🖼️ テーブルビューのプレビュー

```
┌────────────┬────────────┬───────────┬──────────┬────────────┬──────────┬──────────┬──────────┐
│ メッシュID │ 総合スコア │ Pitch     │ Pitch    │ Yaw        │ Yaw      │ EAR      │ EAR      │
│            │            │ スコア    │ 角度     │ スコア     │ 角度     │ スコア   │ 値       │
├────────────┼────────────┼───────────┼──────────┼────────────┼──────────┼──────────┼──────────┤
│ 33         │ 60点       │ 30点      │ 5.6°     │ 30点       │ 2.7°     │ 0点      │ 0.079    │
│ 37         │ 46点       │ 13点      │ 34.3°    │ 0点        │ 174.9°   │ 34点     │ 0.234    │
│ 20         │ 37点       │ 30点      │ 17.1°    │ 0点        │ 166.4°   │ 7点      │ 0.167    │
└────────────┴────────────┴───────────┴──────────┴────────────┴──────────┴──────────┴──────────┘
```

## 📈 スコア計算と検出値の関係

### Pitch（上下方向）の例
```
検出値: Pitch角度 = 5.6°
評価: |5.6| ≤ 20° → 満点 (1.0)
スコア: 30 × 1.0 = 30点
```

### Yaw（左右方向）の例
```
検出値: Yaw角度 = 174.9°
評価: |174.9| ≥ 45° → 0点 (0.0)
スコア: 30 × 0.0 = 0点
```

### EAR（目の開き具合）の例
```
検出値: EAR値 = 0.234
評価: 0.234 ≥ 0.25 に近い → 高スコア (0.85)
スコア: 40 × 0.85 = 34点
```

## ✅ テスト結果

### 1. detect.py の実行テスト
```bash
$ python detect.py --weights yolov7.pt --source input/S__2138118_0.jpg --save-faces
✅ CSV に検出値を含めて出力
```

### 2. CSV フォーマット確認
```bash
$ cat images/results.csv
メッシュID,パス,総合スコア,Pitchスコア,Yawスコア,EARスコア,Pitch角度(度),Yaw角度(度),EAR値
33,face_id_33_20251208-170833_0.jpg,60,30,30,0,5.6,2.7,0.079
✅ すべての値が含まれている
```

### 3. Flask 読み込みテスト
```python
from app3 import load_results_from_csv
results = load_results_from_csv()
# ✅ pitch_angle, yaw_angle, ear_value が正常に読み込まれた
```

### 4. HTML テーブル表示確認
```
メッシュID | 総合スコア | Pitch | Pitch角度 | Yaw | Yaw角度 | EAR | EAR値 |
33         | 60点      | 30点  | 5.6°     | 30点 | 2.7°   | 0点 | 0.079 |
✅ すべての値が表示されている
```

## 🎯 利点

### 1. **透明性の向上**
- 評価の背景にある実際の測定値を確認可能
- スコアと検出値のズレを検証できる

### 2. **詳細分析**
- Pitch/Yaw/EAR の詳細な数値で傾向分析
- 複数人の比較分析が容易

### 3. **デバッグ支援**
- スコア計算ロジックの検証
- 境界値（±20°、EAR 0.15～0.25）の確認

### 4. **データエクスポート**
- CSV には全情報が記録されているため
- 外部ツール（Excel、Python など）での分析に活用可能

## 📝 使用例

### ブラウザでの表示
1. `http://localhost:5001` にアクセス
2. 画像をアップロード → 「検出実行」をクリック
3. テーブルビューに以下の情報が表示：
   - **スコア** - 評価点数（0～100点）
   - **検出値** - 実際に測定された角度・値

### CSV ファイルの活用
```python
import pandas as pd

df = pd.read_csv('detect_yolo/images/results.csv')
print(df[['メッシュID', 'Pitch角度(度)', 'Yaw角度(度)', 'EAR値']])

# 角度の分布を分析
print(f"Pitch角度の平均: {df['Pitch角度(度)'].mean():.1f}°")
print(f"Yaw角度の最大値: {df['Yaw角度(度)'].max():.1f}°")
```

## 🔄 バージョン履歴

| バージョン | 日付 | 変更内容 |
|-----------|------|---------|
| v1.0 | 2024/12/08 | スコア計算バグ修正（30/30/40点統一） |
| v1.1 | 2024/12/08 | **検出値表示機能追加（本アップデート）** |

## 📦 ファイル修正一覧

- ✅ `detect_yolo/detect.py` (line 128, 376-389)
- ✅ `UI/app3.py` (line 31-44)
- ✅ `UI/templates/index.html` (line 304-325)

## 🚀 次のステップ（推奨）

- [ ] グラフ表示（角度分布、スコア分布）
- [ ] フィルター機能（良好/普通/不良で絞り込み）
- [ ] CSV ダウンロード機能
- [ ] 複数画像一括処理
- [ ] 詳細情報モーダル（顔画像展開時に検出値も表示）

